{
  "name": "pekata-backoffice",
  "version": 2,
  "buildCommand": "rm -rf node_modules package-lock.json pnpm-lock.yaml && npm install --no-optional --no-audit --no-fund && npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install --no-optional --no-audit --no-fund",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        },
        {
          "key": "X-Frame-Options",
          "value": "DENY"
        },
        {
          "key": "X-XSS-Protection",
          "value": "1; mode=block"
        }
      ]
    }
  ]
}
